# A basic UI for this blog site.

## Initializing

We have our main App component that will contain all our blog posts.
For storing our blogs, we initialized a state variable called `blogsData` with `useState`.
It will be an array of objects and those objects will be the data of our individual blog.
